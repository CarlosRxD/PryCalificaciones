<resources>
    <!-- Tema de la app -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- Título principal -->
    <style name="TitleText">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Texto de cuerpo / etiquetas -->
    <style name="BodyText">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- EditText genérico en login -->
    <style name="LoginEditText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Botón primario estilizado -->
    <style name="LoginButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">24dp</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <!-- Elevación predeterminada heredada de MaterialComponents → no es necesario definir animator aquí -->
    </style>

    <!-- Botón de texto secundario -->
    <style name="ForgotPasswordButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textColor">?attr/colorSecondary</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>
</resources>